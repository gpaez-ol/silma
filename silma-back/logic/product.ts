import { ProductAttributes } from "database/models";
/*import { writeToConsole } from "utils";
import { ProductType, ProductGender, ProductFormat, ProductLanguaje, ProductStatus, ProductItem} from "types";

//Get articulos
export const getArticlesList = (products: ProductAttributes[]): ProductItem[] =>{
    const articles: ProductItem[] = products.filter((products) => { 
        products.type == "article"
    }).map((products)=>{
        return{
            
        }
    })
    return articles
}
//Get libros
export const getBooksList = (products: ProductAttributes[]): ProductItem[] =>{
    const books: ProductItem[] = products.filter((products) => {
        products.type = "book"
    }).map((products)=>{
        return{

        }
    })
    return books
}
*/
//Post productos
//export const postProducts = () => {}